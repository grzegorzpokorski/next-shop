fragment ProductSummary on Product {
  id
  name
  slug
  price
  currency
  gallery(first: 1) {
    ...ImageDetails
  }
  quantityAvailable
  category {
    ...CategoryDetails
  }
  updatedAt
  createdAt
}
