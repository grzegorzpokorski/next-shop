mutation UpdateProductAvailableQuantity($productId: ID!, $quantity: Int!) {
  updateProduct(
    where: { id: $productId }
    data: { quantityAvailable: $quantity }
  ) {
    ...ProductSummary
  }
}
